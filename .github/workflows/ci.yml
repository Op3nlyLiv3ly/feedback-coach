name: CI

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          cd skills/feedback-coach
          npm install
      - name: Run tests
        run: |
          cd skills/feedback-coach
          npm test
